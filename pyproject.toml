[project]
name = "mem0-mcp"
version = "0.1.0"
description = "Personal AI Assistant Memory Management with MCP and Mem0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.3.0",
    "mem0ai>=0.1.55",
    "aiohttp>=3.8.0",
]
